<view class="head">
  <text class="title">我的图片</text>
</view>

<view class="body">
  <view class="picture-wrap" wx:for="{{pictureList}}" wx:key="item.id">
    <image class="picture" src="{{item.url}}" />
    <view class="info">
      <text>{{item.position}}: </text>
      <text>{{item.description}}</text>
    </view>
  </view>
</view>